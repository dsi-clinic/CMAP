import pandas as pd

fiona.open
openfile file_name as data
##
shapefiles 
kane county is gdb
other is shp
each feature has properties and geometries, GIBI - label & id
geometries - type of geometry, GIBI is point with coordinates, and Kane are random polygons, points
Kane county has layers within the gdb file, each layer corresponds to different type or label of what it is.

##
#fiona access what's in the data